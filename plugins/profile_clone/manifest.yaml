api_version: v1
kind: plugin
name: profile_clone
version: "1.0.0"
display_name: "Profile Clone"
description: "Consume scrape output contract and execute clone flow contract"
entry_script: script.yaml
inputs:
  - name: source_key
    type: string
    required: false
    default: "scrape_profile"
